dpkg-deb -R /path/to/.deb /path/to/extract # extract libraries that will be installed using the deb

sudo apt install plocate # locate the config, libs, and bins for an apt package
locate package-name
